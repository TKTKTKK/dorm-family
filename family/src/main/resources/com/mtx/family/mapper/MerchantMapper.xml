<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mtx.family.mapper.MerchantMapper">

    <resultMap id="selectResultMap" type="com.mtx.family.entity.Merchant">
    </resultMap>

    <select id="selectMerchantList" resultMap="selectResultMap">
        select *
        from tb_mtx_merchant
        where bindid = #{merchant.bindid}
        order by modifyon desc;
    </select>

    <select id="selectMerchantForSave" resultMap="selectResultMap">
        select c.* from tb_mtx_merchant c
        where c.name = #{merchant.name}
        <if test="merchant.uuid != null and merchant.uuid != ''">
            and c.uuid != #{merchant.uuid}
        </if>
        ;
    </select>

    <select id="selectMerchantForUser" resultMap="selectResultMap">
        select c.*
        from tb_platform_user a, tb_mtx_user_merchant b, tb_mtx_merchant c
        where a.uuid = b.userid
        and   b.merchantid = c.uuid
        and   a.uuid = #{userid}
        order by c.createon;
    </select>

    <select id="selectMerchantForUserWithPagination" resultMap="selectResultMap">
        select c.*
        from tb_mtx_merchant c
        where bindid = #{merchant.bindid}
        <if test="topAccount == ''">
            and c.uuid in (select c.uuid
            from tb_platform_user a, tb_mtx_user_merchant b, tb_mtx_merchant c
            where a.uuid = b.userid
            and   b.merchantid = c.uuid
            and   a.uuid = #{userid}
            )
        </if>
        order by modifyon desc;
    </select>

</mapper>